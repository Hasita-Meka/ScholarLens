# ScholarLens Environment Configuration
# Copy this file to .env and fill in your values

# Database URL (PostgreSQL recommended, SQLite works for local testing)
# For PostgreSQL:
DATABASE_URL=postgresql://username:password@localhost:5432/scholarlens

# For SQLite (simpler for local testing - no setup needed):
# DATABASE_URL=sqlite:///scholarlens.db

# OpenAI API Key (required for AI features like Q&A and summaries)
OPENAI_API_KEY=your-openai-api-key-here
